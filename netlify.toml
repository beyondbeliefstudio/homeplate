[build]
  command   = "npm run build"
  publish   = "dist"
  functions = "netlify/functions"

[functions."consolidate-pantry"]
  timeout = 15

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
