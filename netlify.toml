[build]
  command   = "npm run build"
  publish   = "dist"
  functions = "netlify/functions"

[dev]
  command    = "npm run dev"
  targetPort = 8888

[functions."consolidate-pantry"]
  timeout = 15

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
